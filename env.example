# Database
# Get your connection string from your PostgreSQL provider (Vercel Postgres, Supabase, Neon, etc.)
# Format: postgresql://user:password@host:port/database?sslmode=require
DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"

# JWT Secret for session encryption
# Generate a random string: openssl rand -base64 32
JWT_SECRET="your-secret-key-change-in-production"

# Admin Credentials (optional - defaults are set in code)
ADMIN_USERNAME="bigguy"
ADMIN_PASSWORD="!RY7!@gak"


